# NetOps Analytics Dashboard

**End-to-end cloud network operations analytics platform** — monitoring, KPIs, anomaly detection, and reporting across a simulated 5-region AWS/Azure-style infrastructure.

---

## What this project does

| Layer | What it does |
|-------|-------------|
| Data simulation | Generates 30 days of realistic cloud network metrics (latency, packet loss, uptime, incidents) |
| Processing pipeline | Cleans, enriches, and aggregates raw data into structured CSVs |
| Analytics engine | Computes KPIs, runs Z-score anomaly detection, estimates business impact |
| Dashboard | Streamlit app + standalone HTML dashboard with charts and interactive filters |

---

## Folder structure

```
netops_dashboard/
├── pipeline/
│   ├── simulate_data.py      # Stage 2: Data simulation
│   └── process_data.py       # Stage 3: Processing pipeline
├── analytics/
│   └── compute_kpis.py       # Stage 4: KPI engine + anomaly detection
├── dashboard/
│   └── app.py                # Stage 5: Streamlit dashboard
├── data/
│   ├── raw/                  # Generated by simulate_data.py
│   │   ├── network_metrics.csv
│   │   ├── uptime_logs.csv
│   │   ├── incidents.csv
│   │   └── simulation_summary.json
│   └── processed/            # Generated by process_data.py
│       ├── metrics_clean.csv
│       ├── metrics_hourly.csv
│       ├── metrics_daily.csv
│       ├── uptime_clean.csv
│       ├── uptime_by_region.csv
│       ├── incidents_clean.csv
│       ├── incidents_daily.csv
│       ├── incidents_by_region.csv
│       └── incident_types.csv
└── reports/                  # Generated by compute_kpis.py
    └── analytics_report.json
```

---

## Quick start

### 1. Install dependencies

```bash
pip install pandas numpy streamlit plotly
```

### 2. Run the full pipeline

```bash
# Step 1: Simulate 30 days of network data
python pipeline/simulate_data.py

# Step 2: Clean and process raw data
python pipeline/process_data.py

# Step 3: Compute KPIs and detect anomalies
python analytics/compute_kpis.py

# Step 4: Launch the dashboard
streamlit run dashboard/app.py
```

---

## System architecture

```
[Simulate Data] → data/raw/
      ↓
[Process Pipeline] → data/processed/
      ↓
[Analytics Engine] → reports/analytics_report.json
      ↓
[Streamlit Dashboard] ← reads processed/ + report JSON
```

---

## Metrics explained

| Metric | Why it matters |
|--------|---------------|
| **Latency (ms)** | Directly impacts user experience. >150ms = SLA breach risk |
| **Packet loss %** | Indicates congestion or hardware failure. >1% = alert threshold |
| **Uptime %** | Core SLA metric. Target: 99.9% (enterprise standard) |
| **MTTR (min)** | Mean Time to Resolve — measures ops team efficiency |
| **Health score** | Composite 0–100 score from latency + loss. Used for quick status |
| **Z-score anomaly** | Flags values >3σ from mean — triggers automated alerts |

---

## Key KPI results (simulated)

| KPI | Value |
|-----|-------|
| Overall uptime | 99.48% |
| Avg latency | 32.25 ms |
| P95 latency | 58.45 ms |
| Total incidents (30 days) | 99 |
| Overall MTTR | 204 minutes |
| Anomalies detected | 50 |
| Estimated cost saved | $113,250 |

---

## Tech stack

| Tool | Why |
|------|-----|
| Python + pandas | Industry standard for data pipelines |
| numpy | Efficient numerical simulation |
| Streamlit | Fastest path from data to interactive dashboard |
| Plotly | Production-quality interactive charts |
| JSON | Simple, portable report format |

---

## Design decisions

1. **No database** — CSV + JSON keeps it portable and beginner-friendly. In production this would be InfluxDB or TimescaleDB.
2. **Separate pipeline stages** — simulate → process → analyze is modular. Each script can run independently or be replaced.
3. **Z-score anomaly detection** — simple, explainable, no ML dependencies. Production systems add ML (Isolation Forest, LSTM) on top.
4. **SLA thresholds hardcoded** — in production these come from a config file or database tied to customer contracts.

---

## Real-world mapping

| This project | Real company equivalent |
|-------------|------------------------|
| `simulate_data.py` | AWS CloudWatch / Datadog agent |
| `process_data.py` | Apache Kafka + Spark pipeline |
| `compute_kpis.py` | Grafana alerting + anomaly detection |
| `app.py` | PagerDuty + Grafana dashboard |
| `analytics_report.json` | Elasticsearch index / metrics store |

---

## Author

Built as an end-to-end capstone demonstrating network operations analytics skills.  
Stack: Python · pandas · Streamlit · Plotly · Z-score anomaly detection
